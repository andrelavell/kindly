services:
  - type: web
    name: kindly
    env: node
    buildCommand: npm run build
    startCommand: npm start
    nodeVersion: 18.x
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
